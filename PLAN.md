# Nginx UIX 版本迭代计划

> 文档状态：已确认
>
> 制定日期：2026-07-14
>
> 目标版本：v1.0.0
>
> 本文是项目唯一的长期路线图，直接记录版本范围、交付物和验收条件；不再维护独立设计文档。

## 1. 产品目标

构建一套面向单台服务器的开源 Nginx Web 管理工具，提供安全的配置管理、结构化 upstream/location 管理、真实路径匹配测试、Let's Encrypt 证书自动化以及完整的 Web UI。

产品的核心价值不是简单地把 Nginx 配置搬到网页上，而是形成下面这个安全闭环：

```text
编辑工作副本
    -> 查看差异
    -> 校验完整配置
    -> 在隔离环境测试路径
    -> 创建备份
    -> 原子发布
    -> reload 并确认状态
    -> 失败时自动回滚
```

## 2. 已确认的范围

| 编号 | 功能 | 首次交付版本 | v1.0 完成标准 |
| --- | --- | --- | --- |
| 1 | Nginx 进程状态检查 | v0.1 | 可查看真实进程、版本、PID、worker、最近操作结果，并可安全 reload/restart |
| 2 | 配置文件管理 | v0.2.1–v0.2.3 | 支持发现、分组、编辑、差异、校验、备份、发布和回滚 |
| 3 | upstream 管理 | v0.3 | 支持结构化 CRUD，且不破坏未识别的原始配置 |
| 4 | 匹配路径规则管理 | v0.3 | 支持常用 location 类型、嵌套结构和冲突提示 |
| 5 | 匹配路径测试 | v0.4 | 可在隔离 Nginx 中以 Host + URI 发起真实请求并识别最终命中的规则 |
| 6 | Let's Encrypt 证书管理 | v0.5 | 支持 HTTP-01 与 Cloudflare API Token DNS-01 签发、绑定、续期、部署和失败回滚 |
| 7 | 前端 UI | 全版本 | 每个后端能力在同一版本交付可操作、可反馈状态的 UI |

## 3. v1.0 明确不包含的范围

以下能力不进入 v1.0，避免首版失控：

- 多节点或集群集中管理。
- UI 与 Nginx 分拆成两个 Docker 容器的官方部署方式。
- 管理宿主机或其他容器中的 Nginx。
- Kubernetes Ingress 管理。
- Nginx Plus 专属 API 和动态 upstream 管理。
- WAF、流量分析、访问日志分析和告警平台。
- Cloudflare 之外的 DNS-01 服务商、通用 DNS 插件框架和凭据脚本。
- 插件市场、脚本市场或任意命令执行能力。
- TCP/UDP `stream` 路径测试；v1.0 的路径实验室只处理 HTTP/HTTPS。

原生 Linux 部署可以在 v1.0 之后复用运行时适配层实现。若选择 Docker，官方只推荐并维护一体化镜像。

## 4. 全局约束

- 官方最终运行环境是单机 Linux 上的一体化 Docker 容器。
- v0.1.0 至 v0.6.0 只实施和验证各自的产品、领域、API、前端与原生进程能力，不执行 Docker 构建、容器运行、卷、升级、故障注入、浏览器容器化或多架构验证，也不以 Docker 结果作为版本完成门槛。
- 全部 Docker 验证统一在 v0.7.0 执行；v0.7.0 必须以当时的完整源码重新覆盖 v0.1.0 至 v0.6.0 已交付能力，不能把早期版本的历史 Docker 记录替代为 v0.7.0 通过证据。
- v0.1.0 与 v0.2.1 已经产生的 Docker 验收记录作为不可改写的历史事实保留，但不再定义后续版本的验证顺序或完成条件。
- 镜像内同时运行 Nginx UIX 和由它管理的 Nginx。
- Nginx 配置文件是配置真源；SQLite 不保存另一份可覆盖文件的配置副本。
- 所有生产配置变更都必须经过工作副本、校验、备份和原子替换。
- 任何校验失败都不得 reload 生产 Nginx。
- 任何 reload 后的确认失败都必须保留恢复入口，并在能够安全判定时自动回滚。
- 路径测试必须在独立 PID、prefix、日志、临时目录和回环端口中运行，不能 reload 生产实例。
- Web/API 进程不提供任意 Shell 接口；特权操作只能通过类型化、白名单化的本地控制接口执行。
- 容器不得要求挂载 Docker Socket。
- UI 从 v0.1 起必须具备身份认证、CSRF 防护和安全会话配置。
- 所有前端实现优先复用现有 token 和组件，不为单个功能新增独立设计文档。
- 新建代码文件必须遵守项目 `AGENTS.md` 中的 `@author` 和 `@since` 文件头规则。
- 工程骨架、Go 实现、前端实现、Docker、安全和质量门禁统一遵守根级 `AGENTS.md`。
- 项目初始化时建立 `VERSION` 文件，初始版本为 `0.1.0`，供后续代码文件的 `@since` 使用。

## 5. 总体架构

### 5.1 技术栈

- 后端：Go。
- 前端：Vue 3 + TypeScript。
- 持久化：SQLite。
- API：版本化 REST API；长任务状态使用 SSE 推送。
- 部署：多阶段构建的一体化 Docker 镜像。
- 代理服务：镜像内固定并锁定一个 Nginx stable 版本；生产实例和路径沙箱使用同一二进制。

### 5.2 容器进程模型

```text
Container PID 1 / process supervisor
├── nginx-uix          非特权 Web/API 服务
├── nginx-uix-agent    最小权限的本地控制服务
└── nginx              Nginx master/worker
```

约束如下：

- PID 1 负责信号转发、孤儿进程回收和服务监督。
- UI 崩溃时，监督器可以单独重启 UI，不主动停止仍然健康的 Nginx。
- Nginx 异常退出时，状态必须立即反映在 UI；自动拉起策略应有最小重启间隔和滚动窗口死亡次数限制。
- `nginx-uix-agent` 只接受状态查询、配置校验、原子发布/恢复、reload 和 restart 等固定操作。
- Agent 通过本地 Unix Socket 服务，不监听容器外网络。
- UI 管理员属于受信任的服务器管理员；审计日志必须记录所有特权操作。

### 5.3 端口与持久化

默认暴露：

- `80/tcp`：Nginx HTTP。
- `443/tcp`：Nginx HTTPS。
- `9000/tcp`：Nginx UIX 管理界面，可通过环境变量修改。

持久化目录：

- `/etc/nginx`：Nginx 配置及 UIX 管理的 include 文件。
- `/var/lib/nginx-uix`：SQLite、备份索引、任务状态和审计数据。
- `/var/lib/nginx-uix/certs`：ACME 账户、证书和私钥；可单独挂载，也可包含在上级数据卷中。

容器启动时只在 `/etc/nginx` 完全为空的情况下写入镜像内置的最小默认配置；只要目录中已有内容，就不得自动覆盖。检测到缺少入口文件等不完整状态时应停止启动并给出恢复说明，而不是猜测性修复用户配置。

Nginx 访问日志和错误日志默认输出到容器标准输出/错误；需要文件日志时由用户在配置中显式启用并挂载目录。

### 5.4 模块边界

| 模块 | 职责 | 不负责 |
| --- | --- | --- |
| Runtime | 查询 Nginx 状态，执行 validate/reload/restart | 解析或修改配置内容 |
| Config Workspace | 文件发现、逻辑分组、工作副本、差异、备份、发布和回滚 | 理解 location 匹配语义 |
| Nginx AST | lossless 解析、source span、结构查询和局部修改 | 写数据库或控制进程 |
| Upstream | upstream 结构化模型、校验和局部变更 | 主动健康检查平台 |
| Location | server/location 结构化模型、优先级提示和局部变更 | 发起真实测试请求 |
| Route Lab | 静态分析、沙箱构建、请求执行和结果解释 | 发布生产配置 |
| Certificate | ACME 账户、HTTP-01、Cloudflare API Token DNS-01、签发、续期、绑定和证书文件部署 | 通用 DNS 服务商管理与任意凭据脚本 |
| Store/Audit | 用户、会话、分组元数据、任务和审计 | 保存 Nginx 配置真源 |
| Web/API | 权限校验、输入验证、任务编排和 UI | 绕过领域模块直接执行命令 |

### 5.5 目标目录结构

下列结构是后续详细计划的默认边界；只有经过设计评审才调整：

```text
cmd/
  nginx-uix/             Web/API 入口
  nginx-uix-agent/       本地特权控制入口
internal/
  runtime/               Nginx 进程与命令适配
  config/                工作副本、差异、备份和发布事务
  nginxast/              tokenizer、AST、source span 和局部渲染
  upstream/              upstream 领域逻辑
  location/              server/location 领域逻辑
  routelab/              静态匹配与隔离运行测试
  certificate/           ACME 与证书部署
  store/                 SQLite、迁移和审计
  httpapi/               REST、SSE、认证和授权
web/                     Vue 3 + TypeScript 前端
deploy/docker/           一体化镜像、初始化和健康检查
tests/fixtures/nginx/     Nginx 配置及匹配规则测试样本
VERSION                  项目版本号
```

## 6. 核心工作流

### 6.1 配置发布事务

1. 从当前配置版本创建工作副本，并记录内容摘要。
2. 在工作副本中执行编辑或结构化变更。
3. 展示逐文件差异和受影响的 include 关系。
4. 对完整候选配置执行 `nginx -t`，而不是只检查单个片段。
5. 校验摘要，防止其他会话或外部程序已经修改生产文件。
6. 创建不可变备份和变更审计记录。
7. 使用同文件系统临时文件、`fsync` 和 rename 完成原子替换。
8. 对生产配置再次执行 `nginx -t`。
9. reload Nginx，并确认 master PID、配置测试结果和基础 HTTP 健康状态。
10. 操作失败时恢复备份；回滚结果必须明确显示为成功、失败或需要人工处理。

### 6.2 路径测试

路径实验室接受以下输入：

- 候选配置版本。
- Scheme、Host、端口和 SNI。
- HTTP Method、URI、Query、Headers 和可选 Body。
- 超时、期望状态码和可选响应文本断言。

测试流程：

1. 解析 server/listen/server_name/location，生成静态候选匹配说明。
2. 将完整候选配置复制到临时工作区并解析 include 依赖。
3. 把 listen 映射到随机回环端口，重写 PID、日志和临时目录。
4. 在临时配置中为 server/location 注入只用于测试的唯一 Route ID。
5. 对沙箱配置执行 `nginx -t`。
6. 启动第二个 Nginx master，并等待回环端口就绪。
7. UI 后端向沙箱发送真实请求，保留 Host 和 TLS SNI 语义。
8. 返回静态分析、最终 Route ID、响应状态、关键响应头、响应片段、最终 upstream 和耗时。
9. 终止沙箱并清理 PID、Socket、日志和临时文件；超时或取消也必须清理。

v0.4 保证识别最终命中的 server/location。对于 rewrite、try_files 和 error_page 造成的完整内部跳转链，只展示能够通过静态分析或运行日志可靠确认的部分，不把推测包装成事实。

非幂等方法和携带 Body 的请求必须二次确认，因为真实测试可能访问实际 upstream 并产生副作用。请求目标固定为沙箱回环地址，用户不能利用该入口直接填写任意目标 URL。

### 6.3 ACME 证书生命周期

1. 创建或导入 Let's Encrypt ACME 账户。
2. 使用测试环境预检域名；HTTP-01 校验 challenge 路径，Cloudflare DNS-01 校验 Token 权限与目标 Zone。
3. HTTP-01 通过事务化配置临时启用 challenge 响应；Cloudflare DNS-01 只创建任务拥有的精确 TXT 记录并等待权威 DNS 可见。
4. 完成签发并以严格权限写入证书目录。
5. 修改目标 server 的证书引用，执行完整配置校验。
6. 原子部署证书和配置并 reload。
7. 失败时恢复旧证书、旧配置，并清理 HTTP challenge 临时配置或任务创建的 Cloudflare TXT 记录。
8. 后台任务按到期时间提前续期，使用抖动避免所有证书同时执行。

v1.0 承诺 HTTP-01，以及仅使用 Cloudflare API Token 的 DNS-01 和通配符证书。其他 DNS 服务商、Global API Key 和通用插件机制不进入 v1.0。

## 7. 版本迭代

### v0.1.0：基础可观测版

**目标：** 一条命令启动一体化容器，并在受保护的 Web UI 中可靠查看内部 Nginx 的真实状态。

**验收证据：** [v0.1.0 验收记录](docs/release/v0.1.0-verification.md)

**代码审核修复（2026-07-15）：** 本次单一修复提交已关闭 [H-1、M-1、M-2](docs/review/2026-07-15-v0.1-review-findings.md)，其中 H-1 已完成生产 `fs.Sub` 形态的根路径二次验收；RED/GREEN 过程、真实 Nginx、固定 Node/npm 前端门禁和完整一体化 Docker smoke 见[补充验收](docs/release/v0.1.0-verification.md#2026-07-15-代码审核修复补充验收)与[实施计划](docs/superpowers/plans/2026-07-15-v0.1-review-fixes.md)。

本节中的 Docker 条目记录 v0.1.0 已实现的部署能力和历史交付事实；按当前版本策略，其统一 Docker 复验属于 v0.7.0，不作为 v0.1.0 或后续 v0.7.0 之前版本的新增验证任务。

**交付物：**

- [x] 初始化 Go、Vue 3、TypeScript、SQLite 和 `VERSION`。
- [x] 建立可复现的多阶段 Docker 构建。
- [x] 实现空配置卷初始化，并保护非空 `/etc/nginx` 不被镜像默认值覆盖。
- [x] 实现 PID 1 监督、信号转发和优雅停止。
- [x] 同时启动 UI、Agent 和 Nginx，分别采集健康状态。
- [x] 实现单管理员初始化、登录、登出、安全 Cookie 和 CSRF 防护。
- [x] 实现 Runtime 接口及 Docker 一体化适配器。
- [x] 展示 Nginx 版本、编译参数、master PID、worker PID、启动时间和运行状态。
- [x] 展示最近一次启动配置检查和受限自动恢复结果；v0.1 不提供手动 reload/restart 控件。
- [x] 通过 `nginx -T` 只读展示实际生效配置、文件清单和加载顺序，不扫描未生效文件。
- [x] 提供 UI Dashboard、登录页和只读配置浏览页。
- [x] 提供同时覆盖 UI 与 Nginx 的容器健康检查。
- [x] 产出 amd64 与 arm64 镜像。

**功能验收条件（Docker 形态统一在 v0.7.0 复验）：**

- 新数据目录可完成初始化并进入 UI。
- 已有非空配置目录不会被默认配置覆盖。
- UI 显示的 Nginx 状态与真实进程一致，而不是仅依赖监督器状态。
- 单独终止 UI 后 Nginx 继续服务，UI 被监督器恢复。
- 单独终止 Nginx 后 UI 明确显示异常，并按既定受限恢复策略处理。
- 应用停止能在超时内优雅停止其拥有的进程且无残留子进程。
- 未登录用户不能读取运行状态或配置内容。

### v0.2.1：安全配置工作区版

**目标：** 在完全不修改生产配置、不执行 reload/restart 的前提下，提供可持久化、可并发保护、可审计的配置编辑与差异工作区。

**验收证据：** [v0.2.1 验收记录](docs/release/v0.2.1-verification.md)

**交付物：**

- [x] 实现配置根目录约束、路径穿越防护，以及敏感材料、symlink、特殊文件和外部依赖的 fail-closed 分类。
- [x] 建立 `base`、`draft`、`control` 分离的持久化工作区；`base` 为不可变受管配置快照，`draft` 为无 hardlink 的受管内容完整可编辑副本。
- [x] 建立确定性文件清单、include 依赖图和 SQLite 逻辑分组；逻辑分组不改变物理目录。
- [x] 支持创建、复制、重命名、编辑和删除受管 UTF-8 文本配置文件。
- [x] 支持原始文本编辑、Nginx 语法高亮、搜索、当前文件差异和全部差异预览。
- [x] 实现生产摘要、草稿摘要、强 ETag 和 `If-Match` 乐观并发控制。
- [x] 实现有界操作日志、原子工作区写入、数据库失败回退、启动重放和工作区对账。
- [x] 提供版本化工作区、文件、分组和差异 API，以及对应的响应式、键盘可操作 UI。
- [x] 记录工作区创建、文件变更、冲突和异常状态的必要审计元数据，不记录配置正文或完整差异。
- [x] 建立 v0.1.0 → v0.2.1 数据升级、持久化重开、故障注入和启动对账测试。

**验收条件：**

- 可从稳定的生产快照创建工作区并完成文件 CRUD、搜索、include 查看和差异审阅。
- 工作区操作前后 `/etc/nginx` 摘要、Nginx master PID 和服务状态保持不变。
- 两个会话同时编辑同一工作区时，后提交者收到冲突且浏览器中的未保存文本不丢失。
- 外部程序修改生产配置后，旧工作区进入 `stale` 并拒绝继续写入，不自动覆盖或合并外部变更。
- 路径穿越、越界 symlink、特殊文件、无效文本和资源超限均 fail closed，且 API 不泄露绝对路径或配置正文。
- 进程在创建或编辑中途崩溃后，不会把半成品工作区报告为 `ready`；能够自动恢复的操作会重放，其余进入 `needs_attention`。
- 应用进程重启并重新打开持久化数据后，工作区和逻辑分组仍然存在且可对账。

v0.2.1 验收记录中已经执行的 Docker、缓存和多架构步骤继续作为历史事实保留；它们不属于当前路线图下 v0.2.1 的完成门槛，v0.7.0 仍须从完整源码统一复验。

### v0.2.2：安全发布闭环版

**目标：** 在 v0.2.1 工作区之上完成候选配置校验、不可变备份、原子发布、reload、健康确认和可判定的自动回滚。

**验收证据：** [v0.2.2 验收记录](docs/release/v0.2.2-verification.md)

**交付物：**

- [x] 对完整候选配置运行隔离且有界的 `nginx -t`，结构化展示可安全公开的错误位置。
- [x] 发布前重新校验生产摘要和工作区摘要，拒绝 stale、冲突或需要人工处理的工作区。
- [x] 创建不可变备份及备份索引，并验证备份完整性后才允许写入生产配置。
- [x] 实现多文件原子发布协议、发布后完整校验和明确的一致性恢复顺序。
- [x] 实现 reload、master PID 与基础 HTTP 健康确认，以及安全可判定时的自动回滚。
- [x] 记录校验、备份、发布、reload、确认和回滚各阶段的审计事件与脱敏结果。
- [x] 提供发布检查、确认、进度、结果和自动回滚状态 UI。
- [x] 建立 reload 失败、生产并发修改、只读目录、磁盘满、进程崩溃和升级恢复测试。

**验收条件：**

- 错误候选配置不能覆盖生产文件，也不能触发 reload。
- 有效候选配置只在摘要未变化且备份已验证时原子发布，并在 UI 中显示各阶段证据。
- reload 或健康确认失败时，系统恢复到最后一个有效版本；不能安全判定时进入 `needs_attention`，绝不报告成功。
- 发布过程中断后可从持久化状态恢复，不会把半发布配置或不完整备份标记为成功。
- 应用与 Agent 进程重启并重新打开持久化数据后，配置、备份索引和发布结果仍然存在。

### v0.2.3：恢复与运行控制版

**目标：** 补齐人工恢复、备份保留、restart、完整历史审计和难以自动判定故障的运维处置闭环。

**验收证据：** [v0.2.3 验收记录](docs/release/v0.2.3-verification.md)

**交付物：**

- [x] 实现按不可变备份执行的人工恢复，并在恢复前后校验配置与运行状态。
- [x] 实现有界、可审计且不删除受保护恢复点的备份保留策略。
- [x] 实现类型化 restart 操作、超时、取消、健康确认和失败恢复。
- [x] 提供配置历史、备份、审计、人工恢复和运行控制页面。
- [x] 为 `needs_attention` 提供证据、只读保护、恢复入口和人工处置结果记录。
- [x] 强化数据升级、备份损坏、端口冲突、Agent/UI/Nginx 进程崩溃及原生恢复验收。

**验收条件：**

- 管理员可以从已验证备份恢复配置，恢复失败不会破坏当前最后一个已知有效版本。
- restart 只通过 Agent 的固定类型化操作执行，结果与真实 Nginx 进程状态一致。
- 保留策略在空间约束下确定性运行，不删除当前版本、正在使用的恢复点或法定保护项。
- `needs_attention` 不会被普通编辑或发布绕过，处置前后均保留审计证据。
- 应用升级、进程重启和故障恢复后，配置、数据库、备份、历史和审计保持一致。

### v0.3.0：结构化 upstream 与 location 版

**目标：** 在不破坏手写配置的前提下，以结构化方式管理 upstream 和路径规则。

**验收证据：** [v0.3.0 验收记录](docs/release/v0.3.0-verification.md)

**交付物：**

- [x] 实现带 source span 的 tokenizer 和 lossless AST。
- [x] 建立解析、局部修改、渲染和再次解析的一致性测试。
- [x] 支持发现跨文件的 `http`、`server`、`upstream` 和 `location` 块。
- [x] 支持 upstream 创建、重命名、删除和引用检查。
- [x] 支持 upstream server 的地址、端口、weight、backup、down、max_fails 和 fail_timeout。
- [x] 保留 UI 暂不识别的 upstream 指令和参数。
- [x] 支持 location 精确匹配、普通前缀、`^~`、`~`、`~*` 和 named location。
- [x] 支持嵌套 location 的读取、编辑和合法性检查。
- [x] 支持 proxy_pass 与 upstream 引用选择。
- [x] 提示重复规则、不可达规则、顺序敏感正则和悬空 upstream 引用。
- [x] 结构化操作前展示生成差异，并允许退回原始文本编辑。
- [x] 提供 Upstream、Server/Location 管理页面。

**验收条件：**

- 只读取并重新保存配置时，不改变原始内容。
- 修改一个目标块时，目标块之外的注释、空白、未知指令和文件内容保持不变。
- 删除被引用的 upstream 会被阻止并列出引用位置。
- 所有 Nginx 官方 location 类型都有正例、反例和优先级测试样本。
- 结构化修改仍然经过 v0.2.2/v0.2.3 的完整发布与恢复事务，不能绕过校验和备份。

### v0.4.0：路径实验室版

**目标：** 在不影响生产 Nginx 的情况下，验证候选配置对指定 Host + URI 的真实匹配结果。

**验收：** [v0.4.0 验收证据](docs/release/v0.4.0-verification.md)

**交付物：**

- [x] 实现 server/listen/server_name 的静态选择分析。
- [x] 实现 exact、prefix、`^~`、regex 和 nested location 的候选分析。
- [x] 展示候选规则、优先级和排除原因。
- [x] 实现 include 依赖复制和沙箱配置重写。
- [x] 实现随机回环端口、独立 PID/prefix/log/temp 目录。
- [x] 为沙箱 server/location 注入稳定且可追溯的 Route ID。
- [x] 支持 HTTP/HTTPS、Host、SNI、Method、URI、Query、Headers 和 Body。
- [x] 展示最终 server/location、最终 URI、upstream、状态码、响应头、受限响应片段和耗时。
- [x] 支持期望状态码、期望文本和禁止文本断言。
- [x] 支持用户取消、全局超时、输出大小限制和异常清理。
- [x] 对非幂等请求和真实 upstream 副作用给出明确确认。
- [x] 提供路径实验室、历史结果和可复制测试参数页面。

**验收条件：**

- 测试期间生产 Nginx 的 master PID 和监听端口保持不变。
- exact、最长前缀、`^~`、正则顺序、嵌套规则、rewrite、try_files 和 error_page 均有真实 Nginx 集成测试。
- UI 能区分“静态预测”和“运行时确认”，两者不一致时同时展示证据。
- 任何启动失败、请求超时、用户取消或 UI 重启都不会遗留沙箱 Nginx。
- 未发布工作副本可以测试，测试成功不会自动发布。

### v0.5.0：Let's Encrypt 证书自动化版

**目标：** 完成 HTTP-01 与 Cloudflare API Token DNS-01 证书从申请、绑定到自动续期的闭环。

**验收记录：** [v0.5.0 验收证据](docs/release/v0.5.0-verification.md)

**交付物：**

- [x] 实现 ACME 账户创建、导入、停用和服务条款确认。
- [x] 支持 Let's Encrypt staging 与 production 环境，默认先使用 staging 验证流程。
- [x] 实现 HTTP-01 challenge 的事务化配置和清理。
- [x] 实现 Cloudflare API Token 凭据验证、最小权限提示、加密持久化、DNS-01 TXT 创建/权威传播等待/精确清理；不支持 Global API Key。
- [x] 支持单域名、SAN 和仅限 DNS-01 的通配符证书申请。
- [x] 安全保存账户密钥、证书链和私钥，默认权限不高于 `0600`。
- [x] 将证书绑定到一个或多个 server，并展示配置差异。
- [x] 证书发布必须执行 `nginx -t`、原子替换和 reload。
- [x] 实现到期状态、手工续期和自动续期调度。
- [x] 实现续期抖动、重试退避、速率限制提示和失败记录。
- [x] 实现证书导出、解绑和删除前引用检查。
- [x] 提供证书列表、申请向导、绑定、续期历史和任务日志页面。

**验收条件：**

- 自动化测试使用本地 ACME 测试服务，不消耗 Let's Encrypt production 配额。
- HTTP challenge 配置与 Cloudflare TXT 记录无论成功、失败、取消或应用重启都能清理或进入明确的 `needs_attention`。
- 新证书部署失败时继续使用旧证书，不产生半写入文件。
- 应用进程重启并重新打开持久化数据后，续期计划、账户和证书仍然可用。
- 私钥不会出现在普通日志、审计差异或 API 错误中。

### v0.6.0：发布候选版

**目标：** 不再增加产品模块，集中完成安全、恢复、兼容性和发布工程。

**验收记录：** [v0.6.0 验收证据](docs/release/v0.6.0-verification.md)

**交付物：**

- [x] 完成认证、会话、CSRF、速率限制和敏感信息脱敏审计。
- [x] 对 Unix Socket、文件权限和 Agent 操作白名单进行安全测试。
- [x] 完成数据库迁移、备份导出和灾难恢复流程。
- [x] 建立 Nginx 配置样本兼容矩阵和回归测试集。
- [x] 建立从登录到发布、路径测试、签发证书的浏览器端到端测试。
- [x] 完成安装、升级、回滚、备份和故障排查文档。
- [x] 完成 API 错误码、任务状态和 UI 文案一致性检查。

**验收条件：**

- 所有 v0.1 至 v0.5 的验收条件在干净环境重新通过。
- 从上一个版本升级不丢失配置、账户、证书、分组或历史。
- Agent 不接受任意命令、越界路径或未经授权的调用。
- 关键失败场景均返回可操作错误，并明确生产 Nginx 是否仍健康。

### v0.7.0：Docker 一体化统一验证版

**目标：** 不增加产品功能，以 v0.6.0 的完整能力为唯一源码基线，集中完成一体化 Docker 镜像、容器生命周期、持久化升级、故障恢复、安全边界和多架构验证。

**验证边界：** v0.1.0 至 v0.6.0 不执行 Docker 验证。所有历史 Docker 证据只能帮助定位风险，不能替代本版本对最终完整产品的重新验证。

**验收记录：** [v0.7.0 验收证据](docs/release/v0.7.0-verification.md)

**交付物：**

- [x] 固定基础镜像、Go、Node、npm、Nginx、s6-overlay 和构建参数，验证可复现的多阶段一体化镜像。
- [x] 验证空 `/etc/nginx` 初始化、非空目录不覆盖、默认端口、持久化目录和权限检查。
- [x] 验证 PID 1 信号转发、孤儿回收、UI/Agent/Nginx 独立健康检查、单进程崩溃与优雅停止。
- [x] 在同一最终镜像中覆盖登录、状态、工作区、发布/自动回滚、人工恢复/restart、结构化编辑、路径实验室和 ACME 闭环。
- [x] 建立受支持版本到 v0.7.0 的数据卷升级、容器重建、备份恢复和持久化一致性矩阵。
- [x] 注入只读卷、磁盘满、端口冲突、Nginx/reload/restart 失败、UI/Agent/Nginx 崩溃、PID 1 信号与优雅停止、超时和取消。
- [x] 验证非 root UI、Agent Unix Socket、最小文件权限、无 `--privileged`、无 Docker Socket 和逐项最小 capability。
- [x] 验证 Docker 浏览器端到端流程、响应式、键盘操作和容器网络边界。
- [x] 构建并验证 linux/amd64 与 linux/arm64 镜像行为一致性；缺少原生 runner 时如实记录未运行项，不用模拟结果冒充原生通过。
- [x] 验证 source fingerprint、build identity、缓存失效/复用和同一源码测试镜像唯一性。
- [x] 生成 SBOM、扫描已知漏洞，并检查最终镜像不含编译器、源码缓存、包管理缓存、测试密钥或其他构建材料。

**验收条件：**

- 全新数据卷可一条 Docker 命令启动并完成管理员登录，已有非空配置和持久化数据不会被覆盖。
- UI、Agent、Nginx 的状态与崩溃恢复，以及 PID 1 的信号、优雅停止和重建行为与真实进程一致。
- v0.1.0 至 v0.6.0 的全部核心用户闭环在最终镜像中通过，且失败路径不会损坏生产配置或持久化数据。
- 容器停止、升级、重建和故障恢复后，配置、数据库、备份、证书、任务与审计保持一致。
- linux/amd64 与 linux/arm64 产物构建成功，运行证据按实际 runner 能力明确区分通过、未运行和失败。
- 镜像权限、安全边界、SBOM、漏洞门禁和最终内容检查全部通过。

### v1.0.0：稳定版

**目标：** 冻结功能范围，以最小发布门禁完成正式发布。

**交付物：**

- [x] 冻结 REST API v1、配置模型和数据库迁移规则。
- [x] Go 与前端单元测试通过。
- [x] Docker basic smoke 通过。
- [x] 生成 linux/amd64 与 linux/arm64 二进制和 OCI 镜像包。
- [x] 建立 tag 驱动的 GitHub Release 与 GHCR 发布工作流。
- [x] 发布 v1.0.0 源码、镜像与简明使用文档。

**验收条件：**

- `go test ./...` 与前端 lint、typecheck、test、build 通过。
- native amd64 候选完成 basic smoke。
- linux/amd64 与 linux/arm64 的两个程序二进制和镜像均构建成功。
- 安全、权限、漏洞扫描、SBOM、完整浏览器与长时间稳定性验证后续按缺陷独立处理，不阻断 v1.0.0。

## 8. UI 页面演进

| 页面 | 首次版本 | 主要内容 |
| --- | --- | --- |
| 登录 | v0.1 | 管理员由环境变量/Secret 首次引导，UI 提供登录和会话管理 |
| Dashboard | v0.1 | UI、Agent、Nginx 状态、启动校验和自动恢复状态 |
| 生效配置 | v0.1 | `nginx -T` 实际加载文件、顺序和只读内容 |
| 配置工作区 | v0.2.1 | 分组、编辑、搜索、include 依赖、差异和并发冲突 |
| 发布与 reload | v0.2.2 | 完整校验、备份、发布、reload、健康确认和自动回滚 |
| 恢复与 restart | v0.2.3 | 历史、审计、人工恢复、保留策略、restart 和人工处置 |
| Upstream | v0.3 | upstream 和 server 结构化管理 |
| Server/Location | v0.3 | server、location、proxy_pass 和冲突提示 |
| 路径实验室 | v0.4 | 请求输入、静态解释、运行结果和历史 |
| 证书 | v0.5 | ACME 账户、申请、绑定、续期和任务日志 |
| 系统设置/审计 | v0.6 | 安全设置、备份恢复、审计和版本信息 |

## 9. 测试策略

- Go 与前端执行单元测试和正常构建。
- native amd64 镜像执行 basic smoke。
- Buildx 分别产出 linux/amd64 与 linux/arm64 的 OCI 镜像包；Go 同时产出两个架构的程序二进制。
- 不在发布流水线运行 Playwright、security、权限、漏洞扫描、SBOM、故障注入、完整升级恢复或长时间稳定性套件。

## 10. 主要风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| AST 重写破坏手写配置 | 生产故障或用户失去信任 | source span 局部修改、Golden tests、差异确认、备份回滚、原始编辑兜底 |
| 多进程容器生命周期处理错误 | UI 或 Nginx 异常退出、僵尸进程 | 专用 PID 1 监督、独立健康状态、信号与崩溃测试 |
| Web 漏洞扩大为容器特权 | 配置和证书泄漏、远程控制 | 非特权 UI、Unix Socket Agent、固定操作、路径约束、认证审计、无任意 Shell |
| 沙箱误占生产端口或残留进程 | 生产中断或资源泄漏 | 回环随机端口、独立 prefix/PID、上下文取消、进程组清理、启动回收扫描 |
| 路径静态分析与真实 Nginx 不一致 | 给出错误结论 | 明确区分预测与运行证据，以真实沙箱最终 Route ID 为准 |
| 路径测试触发真实 upstream 副作用 | 修改业务数据 | 默认 GET/HEAD、非幂等请求二次确认、超时与 Body 限制、完整审计 |
| ACME production 速率限制 | 无法签发或续期 | staging 预检、退避、续期抖动、清晰展示服务端错误 |
| 卷未挂载导致数据随容器删除 | 配置、证书或历史丢失 | 首次启动检查、显著警告、备份导出、文档和容器验收测试 |

## 11. 通用完成定义

一个版本只有同时满足以下条件才能标记完成：

- Go 与前端单元测试通过。
- Docker basic smoke 通过。
- linux/amd64 与 linux/arm64 二进制和 OCI 镜像包构建成功。
- 用户可见行为变化同步到简明文档。
- 新建代码文件符合 `AGENTS.md` 的文件头要求。

## 12. 版本状态追踪

状态只使用：`未开始`、`进行中`、`受阻`、`已完成`。

| 版本 | 主题 | 状态 | 前置版本 |
| --- | --- | --- | --- |
| v0.1.0 | 基础可观测版 | 已完成 | 无 |
| v0.2.1 | 安全配置工作区版 | 已完成 | v0.1.0 |
| v0.2.2 | 安全发布闭环版 | 已完成 | v0.2.1 |
| v0.2.3 | 恢复与运行控制版 | 已完成 | v0.2.2 |
| v0.3.0 | upstream 与 location | 已完成 | v0.2.3 |
| v0.4.0 | 路径实验室 | 已完成 | v0.3.0 |
| v0.5.0 | Let's Encrypt | 已完成 | v0.4.0 |
| v0.6.0 | 发布候选 | 已完成 | v0.5.0 |
| v0.7.0 | Docker 一体化统一验证 | 已完成 | v0.6.0 |
| v1.0.0 | 稳定版 | 已完成 | v0.7.0 |

更新本计划时应同时：

1. 更新版本状态。
2. 勾选已经具备验证证据的交付物。
3. 在对应发布记录中链接测试证据。
4. 若范围变化，先更新“已确认的范围”和“不包含的范围”，再调整版本。

## 13. 已确认的关键决策

| 日期 | 决策 |
| --- | --- |
| 2026-07-14 | 核心功能限定为进程状态、配置文件、upstream、location、路径测试、Let's Encrypt 和 Web UI |
| 2026-07-14 | 采用风险优先的版本顺序：状态 → 安全配置 → 结构化规则 → 路径实验室 → 证书 → 稳定化 |
| 2026-07-14 | 采用 Go + Vue 3 + TypeScript + SQLite |
| 2026-07-14 | 一体化 Docker 是主要验收和推荐部署方式 |
| 2026-07-14 | 不提供 UI 与 Nginx 分离容器的官方 Docker 方案 |
| 2026-07-14 | Docker 容器只管理镜像内部的 Nginx，不挂载 Docker Socket 管理外部容器 |
| 2026-07-14 | v1.0 的 ACME 初始范围限定为 HTTP-01；DNS-01 和通配符证书后续设计 |
| 2026-07-14 | 根级 `AGENTS.md` 作为全局工程规范；Go 工程骨架和编码约定参考 `$golang-patterns` |
| 2026-07-15 | 将原 v0.2.0 拆分为可独立安装、升级和验收的 v0.2.1 工作区、v0.2.2 安全发布、v0.2.3 恢复与运行控制 |
| 2026-07-15 | v0.2.1 严禁修改生产配置或控制 Nginx；只有 v0.2.2 在完整校验与备份后获得发布和 reload 能力，restart 与人工恢复延后到 v0.2.3 |
| 2026-07-15 | Docker 验收默认复用 BuildKit、Go 和 npm 缓存，同一源码指纹只构建一次测试镜像；冷构建仅用于缓存诊断或明确安排的验收 |
| 2026-07-18 | v0.2.1 接受 `linux/amd64` 与 `linux/arm64` OCI 构建成功作为多架构边界；按用户决定停止后续 multiarch native runtime，未运行部分不声称通过 |
| 2026-07-18 | 新增 v0.7.0 统一承担全部 Docker 构建、容器运行、卷、升级、故障、浏览器容器化和多架构验证；v0.1.0 至 v0.6.0 不再执行或以 Docker 结果作为完成门槛，早期已有记录只作为历史事实保留 |
| 2026-07-21 | v0.5 按产品要求增加仅限 Cloudflare API Token 的 DNS-01 与通配符证书；不扩展为通用 DNS 插件，不接受 Global API Key |
| 2026-07-23 | v0.7 在原生 arm64 完成全量容器验收；amd64 只记录 OCI/SBOM 静态通过并等待真实 runner，不以模拟结果冒充原生通过 |
| 2026-07-23 | v0.7 的 PID 1 门槛固定为真实信号转发、有界优雅停止、零状态退出和同卷重建；不另设强制 `SIGKILL` 场景 |
| 2026-07-31 | 删除独立设计文档；v1.0 发布门禁收敛为单元测试、basic smoke 和 amd64/arm64 构建，安全与扩展验证后续按缺陷处理 |
| 2026-07-31 | v1.0.0 已通过 GitHub Release 与 GHCR 正式发布；多架构 OCI index 同时包含 linux/amd64 与 linux/arm64 |

## 14. 下一步

- [x] 通过 Go 与前端单元测试。
- [x] 通过 Docker basic smoke。
- [x] 构建 linux/amd64 与 linux/arm64 二进制和 OCI 镜像包。
- [x] 验证 tag 驱动的 GitHub Release 与 GHCR 发布工作流。
- [x] 发布 v1.0.0。
