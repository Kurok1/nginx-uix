# Nginx UIX v0.2.1 验收证据

## 结论与边界

- 被测功能源码对应历史压缩前的候选 `9e5543b`，分支 `feat/v0.2-config-management`；其后只进行文档归一与分支提交压缩，不把这类机械整理声明为重复功能验收。
- Go、前端、浏览器、手工响应式、native Docker 和 multiarch 构建证据均已取得；详细结果见下文。
- `linux/amd64` 与 `linux/arm64` OCI 产物均构建成功。用户在两个构建成功后停止 runtime 阶段，因此不声明 multiarch 脚本整体 Exit 0，也不声明 multiarch runtime 通过。
- Docker 浏览器专项中，非 loopback HTTP 环境按浏览器安全模型阻止 Clipboard API。用户决定将其作为 capability skip；localhost mock 已覆盖，产品 fallback 未实现。
- HEAD 上 redundant workspace 复验及独立 Step 7、Step 10 未在停止指令后执行，不把这些步骤单独标为通过。
- 本次不发布镜像、不推送分支、不创建 tag，也不修改外部基础设施。
- 本文只记录摘要、计数、标识和结论；不记录秘密、代理值、配置正文或开发机临时路径。

## 验收环境

| 项目 | 实际值 |
| --- | --- |
| 主机 | Darwin 23.6.0，arm64 |
| Go | `go1.26.4 darwin/arm64` |
| Node.js / npm（依赖门禁） | `v24.17.0` / `11.13.0` |
| Docker Engine | client/server `29.4.0` |
| Docker Buildx | `v0.33.0` |
| Nginx 集成测试 | `1.31.2` |
| 应用版本 | `0.2.1` |

## 依赖、源码卫生与静态工具门禁

| 命令 / 门禁 | 结果 |
| --- | --- |
| `go mod tidy` | PASS，无意外依赖差异 |
| `go mod verify` | PASS |
| `npm ci` | PASS；修复后的 lockfile 已包含在发布候选中 |
| `tests/docker/buildmeta_test.sh` | PASS |
| `tests/docker/cache_test.sh` | PASS |
| `tests/docker/image_test.sh` | PASS |
| Playwright summary gates | PASS |

依赖安装使用项目固定的 Node/npm 版本；宿主机其他 CLI 版本不作为 `npm ci` 环境证据。Docker build metadata、cache、image 与 Playwright 摘要门禁为 DK-01/DK-02 提供直接静态证据。

## Go 门禁

| 命令 | 结果 |
| --- | --- |
| `go test ./...` | PASS |
| `go test -race ./...` | PASS |
| `go vet ./...` | PASS |
| `golangci-lint run` | PASS，0 issue |
| `go test ./tests/integration -count=1` | PASS |

Go 全量测试、race、vet、lint 与真实 Nginx 集成测试均有成功执行证据。本文不根据源码外观外推额外结论。

## 前端与浏览器门禁

| 命令 | 结果 |
| --- | --- |
| `npm --prefix web run lint` | PASS |
| `npm --prefix web run typecheck` | PASS |
| `npm --prefix web run test` | PASS，26 files / 293 tests |
| `npm --prefix web run build` | PASS |
| `npm --prefix web run test:e2e` | PASS，48 passed / 1 conditional skip |

Playwright 的 1 个 conditional skip 属于预定义条件分支；其余 48 个用例通过。未用降低断言或扩大超时替代验收。

### 手工响应式与键盘检查

| 宽度 | 检查范围 | 结果 |
| ---: | --- | --- |
| 1069 CSS px | tree、editor、drawer、modal、task tabs、conflict copy、页面溢出与键盘路径 | PASS |
| 320 CSS px | tree、editor、drawer、modal、task tabs、conflict copy、页面溢出与键盘路径 | PASS |

自动 Playwright/axe 与手工键盘检查分别留有证据；自动结果未替代手工响应式验收。

## Native Docker 镜像复用与运行套件

固定镜像名为 `nginx-uix:0.2.1-acceptance`。native 套件使用 `BUILD_IMAGE=auto` 或 `BUILD_IMAGE=0` 验证镜像身份复用；未使用 `--no-cache`，也未删除现有缓存。

### 精确完整链（源提交 `7a25ab2`）

- source fingerprint：`4cd4e1c60d795cc8e5cfb6900c1435528448fffe50185526631089886a576ea7`
- build identity：`332a61f26fa350f03bb81fa8803c3959684d738d1beaf03d7bea1e22cde19883`
- image digest：`sha256:788309565fbf9d0965175244b1eb2c02ab9e133dd5163dc855ac2af0e41bb6b8`

| 顺序 | 命令 | 结果 | build / cache / duration |
| ---: | --- | --- | --- |
| 1 | `BUILD_IMAGE=auto tests/docker/smoke.sh` | PASS | `build=yes`；12s；daemon 阶段 cached=8 |
| 2 | `BUILD_IMAGE=0 tests/docker/faults.sh` | PASS | 复用固定身份镜像 |
| 3 | `BUILD_IMAGE=0 tests/docker/workspace.sh` | PASS | 复用固定身份镜像 |
| 4 | `BUILD_IMAGE=auto tests/docker/smoke.sh` | PASS | `build=no`；1s |

该链完整覆盖首次构建、faults、workspace 与最终自动复用；最后一次 smoke 明确证明未重建。

### HEAD 身份复验（`9e5543b`）

- smoke：PASS。
- build identity：`1b28e0d7a8d6a5497cd15db8ad6cd8349d58272e494bb4dd27d9bcb988d6398f`。
- image digest：`sha256:28a0f2e5cb00f95903385e1c67a2918db09b3c722e32b6ee013c2065117bea39`。
- 随后的 faults：PASS。
- redundant workspace：用户停止后未继续，不声明该次复验通过；上方 `7a25ab2` 精确完整链已有 workspace PASS 证据。
- smoke 内建的 secret、history 与生产不变性检查均通过；独立 Step 7 没有在停止指令后再次运行。

## Docker 浏览器专项

Docker 浏览器专项整体按既定场景完成。认证、会话、配置工作区和冲突交互等场景 PASS。

非 loopback HTTP 地址上的复制动作受浏览器 secure-context 限制，Clipboard API 被浏览器阻止。处理边界如下：

- localhost mock 对复制交互已有覆盖；
- 非 loopback HTTP 用例记录为 capability skip，而非产品行为 PASS；
- 未给浏览器增加伪造 secure context 的特权参数；
- 未实现 Clipboard fallback；如生产环境出现对应需求，另行修复产品行为。

## 缓存安全与多架构产物

multiarch 使用 OCI 输出验证两个目标平台。amd64 从无可用目标缓存开始并完成 transactional publish；arm64 复用 current cache。有效缓存规模约 504 MiB。

| 平台 | 构建 | manifest digest | cache | runtime |
| --- | --- | --- | --- | --- |
| `linux/amd64` | PASS | `sha256:f9bcca30706be47f43bfcf887f0a187f49c6e2da5a5bddde6c905b96e1cf1618` | none → transactional publish | 未运行（用户接受） |
| `linux/arm64` | PASS | `sha256:2c719a053296f29471809f25c26712e4987d9000dd274375ee6faa7bb26be076` | current reuse | 未运行（用户接受） |

两个 OCI 构建均成功。用户在构建完成后明确停止后续 runtime，因此不写 `multiarch.sh` 整体 Exit 0，也不把 runtime 标记为 PASS。

## 生产不变性与敏感信息边界

- native smoke 内建 secret/history/invariants 检查：PASS。
- 额外的独立生产不变性扫描未在停止指令后执行，不单独声称通过。
- 证据记录已脱敏：不包含口令、会话值、私钥、证书正文、代理值、配置正文、完整差异或绝对临时路径。

## 验收矩阵

| ID | 场景 | 状态 |
| --- | --- | --- |
| WS-01 | 稳定快照 | PASS |
| WS-02 | 快照中生产变化 | PASS |
| WS-03 | 文件闭环 | PASS |
| WS-04 | 两会话并发 | PASS |
| WS-05 | 外部生产变更 | PASS |
| WS-06 | 路径与条目防护 | PASS |
| WS-07 | 资源上限 | PASS |
| WS-08 | 崩溃恢复 | PASS |
| WS-09 | 生产零变更 | PASS |
| WS-10 | 持久化升级 | PASS |
| UI-01 | 响应式与键盘 | PASS |
| UI-02 | 浏览器数据保护 | PASS；HTTP clipboard capability skip 见上文 |
| DK-01 | 镜像复用 | PASS |
| DK-02 | 缓存安全 | PASS |
| DK-03 | 多架构 | 构建 PASS；runtime 未运行（用户接受） |

## 收敛与提交边界

- README 与 PLAN 的版本状态依据上述已取得证据更新。
- 额外的独立收尾复验未在用户停止指令后执行，不将它列为额外成功门禁。
- 本提交前只执行文档机械检查：`git diff --check`、占位符检查、状态检查与 staged diff 检查。
- 本记录不声称用户明确停止的重复测试、multiarch runtime 或独立扫描已经执行。
