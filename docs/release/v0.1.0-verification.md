# Nginx UIX v0.1.0 验收证据

## 结论与边界

- 结果：通过。
- 被测源代码候选：`d7a557ad3fbebd9ea6f81c9c391a954201c14d2f`（`d7a557a`，提交时间 `2026-07-15T13:24:39+08:00`）。
- 证据采集开始时间：`2026-07-15T05:31:19Z`。
- 最后一项候选复验完成时间：`2026-07-15T06:04:30Z`。
- 文档提交只记录验收结果，不改变上述候选的产品代码、依赖锁或镜像输入。
- 原生 `linux/arm64` 的完整运行、故障、浏览器和停止验收通过；`linux/amd64` OCI 产物已从同一候选构建、加载并检查。由于当前 arm64 主机的 Rosetta 会把 `/proc/<pid>/exe` 映射为 `/mnt/rv/[rosetta]`，无法满足 UIX 的真实可执行文件身份校验，amd64 运行验收按项目决定留到真实 amd64 主机执行。
- 本次没有发布镜像、推送分支、创建 tag 或修改外部基础设施。

## 验收环境

| 项目 | 实际值 |
| --- | --- |
| 主机 | macOS 14.7.4（23H420），Darwin 23.6.0，arm64 |
| Docker 主机 | OrbStack，Linux aarch64，内核 `6.19.13-orbstack-gbd1dc07b8cf4` |
| Go | `go1.26.4 darwin/arm64`，项目 toolchain `go1.26.4` |
| Node.js / npm | `v24.17.0` / `11.13.0`，来自本机 nvm |
| Playwright | `1.61.0`，Chromium/Headless Shell `149.0.7827.55` revision `1228` |
| Nginx 集成测试 | `1.31.2` |
| Docker Engine | client/server `29.4.0` |
| Docker Buildx | `v0.33.0`（`f7897a252f6438f45ef3a1ed339f6e5575881327`） |
| golangci-lint | `2.11.4`，由 Go 1.26.4 构建 |
| goimports | `golang.org/x/tools/cmd/goimports`，`golang.org/x/tools v0.48.0`，由 Go 1.26.4 构建 |
| 应用版本 | `VERSION` 与 `web/package.json` 均为 `0.1.0` |

Go 命令统一使用工作树内的 `GOPATH`、`GOMODCACHE`、`GOCACHE` 和 `GOLANGCI_LINT_CACHE`。前端命令统一把 `/Users/kuroky/.nvm/versions/node/v24.17.0/bin` 放在 `PATH` 首位，并只使用项目级 `web/node_modules`。

## 固定构建输入

| 输入 | SHA-256 |
| --- | --- |
| `node:24.17.0-trixie-slim` | `sha256:9022946fb57e6fb6a2503931470deba4b49db02bfc8f587f93007fd33ef54415` |
| `golang:1.26.4-trixie` | `sha256:68b7145ec43d1820b9a56704554b53d1520aa2a15cb5233e374188a31b2a1bce` |
| `nginx:1.30.3-trixie` | `sha256:b6edb43d9e6e3df4914ffee84030c41f84a9a8c38d9af9b0d44ee4ee295a0a2b` |
| `mcr.microsoft.com/playwright:v1.61.0-noble` | `sha256:57b65fdc9ceabe0ef613124c7bbe2babcf9362c4d85e382fe3b03604e84b428a` |
| s6-overlay 3.2.3.0 noarch | `b720f9d9340efc8bb07528b9743813c836e4b02f8693d90241f047998b4c53cf` |
| s6-overlay 3.2.3.0 x86_64 | `a93f02882c6ed46b21e7adb5c0add86154f01236c93cd82c7d682722e8840563` |
| s6-overlay 3.2.3.0 aarch64 | `0952056ff913482163cc30e35b2e944b507ba1025d78f5becbb89367bf344581` |

`goimports` 的 `golang.org/x/tools v0.48.0` module sum 为 `h1:3+hClM1aLL5mjMKm5ovokw9epgRXPuu2tILgismM6RE=`；golangci-lint 2.11.4 的 module sum 为 `h1:GK+UlZBN5y7rh2PBnHA93XLSX6RaF7uhzJQ3JwU1wuA=`。

## 格式化与依赖可复现性

| 命令 | Exit | 结果 |
| --- | ---: | --- |
| `gofmt -d`，范围为 `main...d7a557a` 的 66 个 Go 文件 | 0 | 无差异，未改写文件。 |
| 固定 goimports 运行 `goimports -d`，同一 66 文件范围 | 0 | 无差异，未改写文件。 |
| `go mod tidy -diff` | 0 | 无差异。 |
| `go mod verify` | 0 | `all modules verified`。 |
| 删除 `web/node_modules` 后执行 `npm --prefix web ci` | 0 | 新装 264 个包，审计 265 个包，0 vulnerability。 |
| `npm --prefix web ls --depth=0` | 0 | 所有直接依赖与锁定版本一致。 |
| `git diff --exit-code -- go.mod go.sum` | 0 | Go 依赖文件无漂移。 |
| `git diff --exit-code -- web/package.json web/package-lock.json` | 0 | npm 清单与 lockfile 无漂移。 |

初次 npm 尝试遇到沙箱外缓存不可写和一次 registry 连接重置；改用工作区可写缓存并复用同一 lockfile 后，最终从不存在的 `web/node_modules` 完成了干净安装。没有修改依赖版本、断言或产品文件。

## Go 门禁

| 命令 | Exit | 结果 |
| --- | ---: | --- |
| `go test ./...` | 0 | 9 个包中 7 个测试包通过，2 个包无测试，0 失败。 |
| `go test -race ./...` | 0 | 同一包集合通过，0 race。 |
| `go vet ./...` | 0 | 无诊断。 |
| `golangci-lint run` | 0 | `0 issues`。 |
| `NGINX_UIX_INTEGRATION=1 go test ./tests/integration -count=1 -v` | 0 | 真实 Nginx 的嵌套/重复 include、无效配置无残留 master、取消清理 3 个子场景全部通过；包耗时 0.737s。 |

真实 Nginx 集成测试在受限沙箱内曾因 macOS `sysctlbyname` 权限被拒而退出；保持候选、命令、缓存和 fixture 不变，仅在获准的宿主机执行边界重跑后全部通过。这次诊断没有更改产品代码、断言或超时。

## 前端门禁

| 命令 | Exit | 结果 |
| --- | ---: | --- |
| `npm --prefix web run lint` | 0 | ESLint 无发现。 |
| `npm --prefix web run typecheck` | 0 | `vue-tsc --noEmit` 与 Node 配置 `tsc --noEmit` 通过。 |
| `npm --prefix web run test` | 0 | Vitest 12/12 文件、99/99 测试通过。 |
| `npm --prefix web run build` | 0 | Vite 8.1.4 转换 80 个模块并生成生产构建。 |
| `npm --prefix web run test:e2e` | 0 | Chromium 29/29 Playwright 测试通过。 |

Playwright 首次运行因浏览器 revision 1228 尚未存在而未进入产品断言；使用项目锁定的 Playwright CLI 安装相同 revision 后，受限沙箱内浏览器进程仍被系统以 `SIGTRAP` 终止。保持测试与产品文件不变，在获准的宿主机执行边界运行后 29/29 通过。

发布文档收敛后又并行复跑 `go test ./...`、前端 lint、typecheck 和 99 项 Vitest，四个命令均 exit 0；这次复验没有产生 tracked source 或 lockfile 差异。

## Docker 门禁与产物

最终三项命令均基于候选 `d7a557a`：

| 命令 | Exit | 结果 |
| --- | ---: | --- |
| `tests/docker/smoke.sh` | 0 | 原生 arm64 全量主线验收通过，输出 `Docker smoke (full, native): PASS`。 |
| `IMAGE=nginx-uix:0.1.0-test tests/docker/faults.sh` | 0 | 全部选定故障场景通过，run ID `20260715053450-69448-0f635604`。 |
| `SKIP_EMULATED_AMD64_RUNTIME=1 ... tests/docker/multiarch.sh` | 0 | 双架构 OCI、arm64 全量运行/故障/浏览器验收通过；amd64 运行按已确认边界跳过。 |

多架构命令使用项目级 `.tmp/buildx-cache`，并显式传入获准的 BuildKit 与构建步骤代理；完整有效命令为：

```sh
env SKIP_EMULATED_AMD64_RUNTIME=1 \
  BUILDX_HTTP_PROXY=http://host.docker.internal:47890 \
  BUILDX_HTTPS_PROXY=http://host.docker.internal:47890 \
  BUILD_STEP_HTTP_PROXY=http://host.docker.internal:47890 \
  BUILD_STEP_HTTPS_PROXY=http://host.docker.internal:47890 \
  BUILD_STEP_NO_PROXY=registry.npmjs.org \
  tests/docker/multiarch.sh
```

| 产物 | 摘要 / 结果 |
| --- | --- |
| 源指纹 | `sha256:fa27c4e75d1c2bfc8bfb2e121b3ea8b92988ade2d2c496f4b87aad05cb846bbf` |
| OCI layout root index | `sha256:4921add13d0d6e7cf95adf5b88331ba7dbcaecb7125afd4f528b79af74bb4ddc` |
| OCI multi-platform index | `sha256:9d649fbcfe19adbf8ef5c3363e31026d4a158b5d21e5ac9882173cfe37e7931d` |
| linux/amd64 manifest | `sha256:1a44489c98057e076ad45ed5a7f285c4b81b67e6ab022f9fe1fca1c449d04351`；已构建、加载、平台检查；runtime `skipped_pending_native_amd64`。 |
| linux/arm64 manifest | `sha256:2be3f3d7c187eff5603a9d831c10b84172a94de23be1aa93caa3697d7c67d24b`；已构建、加载、完整运行验收通过。 |
| 本机 smoke 镜像 | `sha256:cac111147f269e5a21cbf8c38ec4076da0fd59a19abc89213a3be41806a1e8f9`，`linux/arm64`。 |

### Docker 场景摘要

| 场景组 | 已验证行为 |
| --- | --- |
| 启动与隔离 | 一体化镜像启动；UI 仅映射宿主机回环 9000；UI、Agent、真实 Nginx 的健康状态分别验证。 |
| 认证边界 | Secret 文件优先；登录、Cookie、Origin、CSRF、限速与未认证访问拒绝通过。 |
| 配置卷 | 空卷只初始化一次；普通文件、隐藏文件、目录和符号链接构成的非空卷在启动与重建后内容和元数据不变。 |
| 进程证据 | 容器内 `/proc` master/worker 身份与 UI 状态一致；`nginx -T` 只返回实际加载配置。 |
| 持久化 | 管理员、会话、限速、SQLite WAL 与配置在容器重建后保留；不同引导变量不能覆盖已有管理员。 |
| 故障隔离 | 无效/缺失配置、只读/满数据目录、9000 占用、Agent 断开、UI 崩溃和第五次 Nginx 死亡均达到规定最终状态。 |
| 停止与恢复 | 三次有界停止通过；无残留候选容器，数据卷重用后 readiness、登录与 WAL 完整性通过。 |
| 镜像卫生 | 发布层不含编译器、源码缓存、浏览器、测试 fixture、临时 Secret 或明文引导密码；日志和 SQLite 不含配置正文。 |
| 多架构 | 同一候选与 lockfile 生成 amd64/arm64 manifest；arm64 init、登录、状态、优雅停止、full smoke、faults、29/29 Playwright 与浏览器层隔离通过。 |

最终清理检查未发现以 `nginx-uix:0.1.0-test` 为 ancestor 的容器，也未发现临时 Buildx builder。验收保留本地镜像和项目级构建缓存，供后续重复验证使用。

## 路由与范围审计

公开 HTTP 面只包含 OpenAPI 声明的七个操作；除此之外只有 SPA 静态资源 fallback：

| 方法 | 路径 |
| --- | --- |
| GET | `/health/live` |
| GET | `/health/ready` |
| POST | `/api/v1/auth/session` |
| GET | `/api/v1/auth/session` |
| DELETE | `/api/v1/auth/session` |
| GET | `/api/v1/system/status` |
| GET | `/api/v1/nginx/effective-config` |

Agent 只在 Unix Socket 上接受五个无 body、无 query 的固定 GET：

| 方法 | 路径 |
| --- | --- |
| GET | `/v1/health` |
| GET | `/v1/status` |
| GET | `/v1/build-info` |
| GET | `/v1/startup-validation` |
| GET | `/v1/effective-config` |

OpenAPI、public mux、Agent 协议常量、固定 client 和契约测试逐项一致。静态审计与聚焦测试确认：

- Go 生产代码不使用 `sh -c`，不接受任意命令、Nginx 参数或任意路径；外部命令使用固定二进制与固定参数。
- v0.1 不存在配置写 API、reload/restart 控件、Web Storage，或 `config`、`nginxast`、`upstream`、`location`、`routelab`、`certificate` 等未来领域包。
- 唯一 Nginx 配置写入是空 `/etc/nginx` 的默认初始化事务；非空目录不写入。
- 敏感日志测试和源码 sink 审计未发现密码、Cookie、CSRF、session digest、Secret 或配置正文进入日志。
- 候选相对 `main` 新增 171 个文件；其中 133 个代码/配置源文件的文件头、author、since 与放置位置逐一检查，0 mismatch；其余 38 个 Markdown、JSON、lockfile、raw fixture、s6 声明元数据和生成内容按规范豁免。

## 故障与清理证据

| 代表性故障 | 结果与最终状态 |
| --- | --- |
| DB 权限 | 宽松目录或数据库文件被拒绝；没有以不安全权限继续启动。 |
| Origin / CSRF | 缺失或不匹配 Origin、错误 CSRF 被拒绝；受保护操作没有执行。 |
| 命令超时 | 返回稳定超时分类，子进程已回收。 |
| 合并输出溢出 | 超过总输出上限即终止并返回稳定分类，子进程已回收。 |
| Agent 断开 | readiness 变为 503，Dashboard 保留可用组件并显示 Agent/Nginx 诊断不可用；Nginx 服务继续，Agent 恢复后状态恢复。 |
| 无效 Nginx 配置 | UI/Agent 保持可用，无 Nginx master，readiness 503，持久化配置不被默认值修改。 |
| UI 崩溃 | Nginx HTTP 持续服务，s6 恢复 UI，随后 readiness 与登录恢复。 |
| 第五次 Nginx 死亡 | 进入永久失败，不启动第六个 master，readiness 保持 503，UI 继续提供诊断。 |
| 浏览器 stale data | Dashboard 和生效配置页各 1/1 聚焦 Playwright 用例通过；失败刷新后保留并标注最后成功数据，显示非颜色提示和 polite live region，axe 通过。补充组件测试 4/4 通过；卸载重挂后无成功响应即为空，local/session storage、Cache Storage 和 IndexedDB 均无持久化。 |
| Docker stop | `docker stop --time 15` 在界限内完成；无候选容器进程残留，重用卷启动后 readiness、登录与 SQLite WAL 完整性通过。 |

聚焦失败复验命令如下；所有候选断言均通过，预览服务最终在 4173 端口无监听：

| 命令 | Exit | 结果 |
| --- | ---: | --- |
| `go test ./internal/store ./internal/httpapi ./internal/runtime -run '^(TestOpenRejectsUnsafeDatabasePaths|TestOpenAPIParsesAndContainsEveryPublicOperation|TestSessionLoginRejectsHostileRequestsBeforeService|TestSessionReadAndDeleteRequireCookieAndCSRF|TestAgentClientCallsFiveFixedGETEndpoints|TestAgentProtocolAcceptsOnlyFiveExactGETEndpoints|TestAgentProtocolRejectsRequestsOutsideTheFixedSurfaceBeforeRuntimeCall|TestCommandTimeoutCancelsAndReapsProcess|TestCommandEnforcesCombinedOutputLimit)$' -count=1 -v` | 0 | DB 权限、路由契约、Origin/CSRF、固定 Agent 面、timeout 和 overflow 全部通过；两个进程故障均以 PID `ESRCH` 证明已回收。 |
| `npx --no-install playwright test e2e/dashboard.spec.ts --grep 'failed manual refresh retains and labels the last successful sample' --workers=1` | 0 | Dashboard stale-data 1/1 通过。 |
| `npx --no-install playwright test e2e/effective_config.spec.ts --grep 'failed config refresh keeps selected content and labels it stale' --workers=1` | 0 | 生效配置 stale-data 1/1 通过。 |
| 两组聚焦 `npx --no-install vitest run` stale/storage 组件用例 | 0 | Dashboard 2/2、生效配置 2/2 通过。 |

## 验收映射

| v0.1.0 交付项 | 主要证据 |
| --- | --- |
| Go、Vue 3、TypeScript、SQLite、VERSION | 固定工具链、Go/前端门禁、SQLite 权限和迁移测试。 |
| 可复现多阶段构建 | 固定 digest、lockfile 零漂移、OCI 源指纹与双架构 manifest。 |
| 空卷初始化和非空卷保护 | smoke 的三阶段内容/元数据摘要与故障场景。 |
| PID 1、信号和优雅停止 | s6 恢复场景、三次有界停止与重建后检查。 |
| UI、Agent、Nginx 独立健康 | public/Agent health 契约、进程证据和故障矩阵。 |
| 单管理员、会话、Cookie、CSRF | Go 安全测试、浏览器测试、Docker 真实登录与持久化。 |
| Runtime 与一体化适配器 | Agent 固定协议、UDS 边界和真实容器状态。 |
| Nginx 状态和进程指标 | `/proc` 交叉验证、Dashboard 与真实进程比较。 |
| 启动检查和受限恢复 | 无效配置、Nginx 五次死亡与永久失败场景。 |
| `nginx -T` 生效配置 | parser/golden/真实 Nginx 集成与浏览器配置页。 |
| 登录、Dashboard、只读配置 UI | 99 个前端测试与 29 个浏览器测试。 |
| 容器健康检查 | readiness 组合语义和 Docker healthy/unhealthy 转换。 |
| amd64 与 arm64 镜像 | 同源 OCI index 和两个 child manifest；arm64 完整运行通过，amd64 原生运行边界如上。 |

## 信息安全说明

本记录只保留命令、版本、摘要、退出码和脱敏结果。没有记录管理员密码、密码文件内容、Cookie、CSRF token、session token、完整 Nginx 配置正文或完整 verbose 日志。测试使用的临时 Secret、容器、临时 builder 和浏览器运行容器均已清理；本地依赖、镜像和 `.tmp/buildx-cache` 仅作为可复用验证缓存保留。
