# Nginx UIX v1.0.0 验收记录

> 状态：发布门禁通过，尚未发布
>
> 记录日期：2026-07-31
>
> 当前版本：`1.0.0`
>
> 验收提交：`1f3860b2256158305105ea4eedcb572888d1dd9a`
>
> GitHub Actions：[`30617303446`](https://github.com/Kurok1/nginx-uix/actions/runs/30617303446)

## 发布门禁

v1.0.0 只使用以下四类证据：

1. Go 单元测试通过。
2. 前端 lint、typecheck、单元测试和生产构建通过。
3. native linux/amd64 镜像通过 Docker basic smoke。
4. linux/amd64 与 linux/arm64 的两个程序二进制和 OCI 镜像包构建成功。

Playwright、权限或安全套件、漏洞扫描、SBOM、故障注入、完整升级恢复和长时间稳定性测试不属于本次发布门禁。

## 标准命令

```sh
go mod verify
go test ./...

npm --prefix web ci
npm --prefix web run lint
npm --prefix web run typecheck
npm --prefix web run test
npm --prefix web run build

IMAGE=nginx-uix:1.0.0-test \
PLATFORM=linux/amd64 \
BUILD_IMAGE=1 \
SMOKE_PROFILE=basic \
tests/docker/smoke.sh

tests/docker/multiarch.sh
```

## 当前证据

| 门禁 | 状态 | 证据 |
| --- | --- | --- |
| Go 单元测试 | 通过 | `Go quality`，58 秒 |
| 前端单元测试与构建 | 通过 | `Web quality`，1 分钟 |
| Docker basic smoke | 通过 | native linux/amd64 runner |
| amd64/arm64 二进制与 OCI 镜像包 | 通过 | `Docker smoke and multi-platform build`，2 分 49 秒 |

完整 workflow 用时 4 分 3 秒。构建 artifact：

- 名称：`nginx-uix-1.0.0-1f3860b2256158305105ea4eedcb572888d1dd9a`
- 大小：91.9 MB
- SHA-256：`a20cf586aedbbf3c534e7d8b93fd65dfd794afd4ef5f1477ed5b753ea4a81ce1`
- 内容：两个架构的 `nginx-uix`、`nginx-uix-agent` 和 OCI 镜像包，共六个文件
- 保留至：2026-08-07

v1.0.0 发布门禁已经完成。tag、GitHub Release 和镜像推送属于单独的发布动作，尚未执行。
