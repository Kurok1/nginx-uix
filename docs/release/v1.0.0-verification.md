# Nginx UIX v1.0.0 验收记录

> 状态：进行中
>
> 记录日期：2026-07-31
>
> 当前版本：`1.0.0`

## 发布门禁

v1.0.0 只使用以下四类证据：

1. Go 单元测试通过。
2. 前端 lint、typecheck、单元测试和生产构建通过。
3. native linux/amd64 镜像通过 Docker basic smoke。
4. linux/amd64 与 linux/arm64 的两个程序二进制和 OCI 镜像包构建成功。

Playwright、权限或安全套件、漏洞扫描、SBOM、故障注入、完整升级恢复和长时间稳定性测试不属于本次发布门禁。

## 标准命令

```sh
go mod verify
go test ./...

npm --prefix web ci
npm --prefix web run lint
npm --prefix web run typecheck
npm --prefix web run test
npm --prefix web run build

IMAGE=nginx-uix:1.0.0-test \
PLATFORM=linux/amd64 \
BUILD_IMAGE=1 \
SMOKE_PROFILE=basic \
tests/docker/smoke.sh

tests/docker/multiarch.sh
```

## 当前证据

| 门禁 | 状态 | 证据 |
| --- | --- | --- |
| Go 单元测试 | 待运行 | 简化后的 GitHub Actions |
| 前端单元测试与构建 | 待运行 | 简化后的 GitHub Actions |
| Docker basic smoke | 待运行 | native linux/amd64 GitHub runner |
| amd64/arm64 二进制与 OCI 镜像包 | 待运行 | Docker Buildx |

只有上述四项全部通过后，才把 v1.0.0 发布门禁标记为完成。tag、GitHub Release 和镜像推送属于单独的发布动作。
