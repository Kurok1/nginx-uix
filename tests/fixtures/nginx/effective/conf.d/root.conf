# @author hanchao <hanchao@66yunlian.com>
# @since 0.1.0

include conf.d/repeated.conf;

server {
    listen 127.0.0.1:{{PORT}};
    server_name localhost;

    location / {
        return 204;
    }
}
