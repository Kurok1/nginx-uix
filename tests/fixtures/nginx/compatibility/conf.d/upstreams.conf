# @author hanchao <hanchao@66yunlian.com>
# @since 0.6.0

upstream primary_backend {
    server 127.0.0.1:18081 max_fails=2 fail_timeout=5s;
    server [::1]:18082 backup;
    server unix:/tmp/nginx-uix-compatibility.sock down;
    keepalive 8;
}
